---
id: 01KX73KCC3AYK4JC9RT9Z0H266
type: feature
key: E1-F2
status: planned
assigneeId: ~
queue: ~
externalSystem: ~
externalId: ~
externalUrl: ~
workType: ~
urgency: ~
impact: ~
appetiteMinutes: ~
estimateMinutes: ~
actualMinutes: ~
templateId: ~
---

# Canonical-backed aggregation

## Goal

Make the canonical `usage.db` the single source of truth for the report's
aggregates: derive `daily_metrics.json` / `session_metrics.json` from a
`GROUP BY` over the store instead of dual-writing them during the parse.

## Definition of Done

- Daily + session metrics come from SQL aggregation over `usage.db`, matching the
  pre-flip numbers on the real corpus.

## Acceptance Criteria

- [ ] Metrics produced by aggregation queries over `usage.db` (E1-F2-S1).
- [ ] No regression vs. the current dual-write output (within rounding).
- [ ] Rotated-away date/session preservation semantics still hold.

## Test Plan

Assert the aggregation query results against the existing fixtures' expected
costs; diff emitted JSON against the dual-write path on the real corpus.

## Notes

Blocked on E1-F1 — depends on trusting the canonical store. Story: **E1-F2-S1**.

## Activity Log


## 2026-07-10 22:50 UTC -- created
Work-item E1-F2 (feature) created with status planned.
