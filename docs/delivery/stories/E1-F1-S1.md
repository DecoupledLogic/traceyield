---
id: 01KX73KK8QDGBA2ZAZQD7A39KB
type: story
key: E1-F1-S1
status: in_review
assigneeId: agent-charles
queue: ~
externalSystem: ~
externalId: ~
externalUrl: ~
workType: ~
urgency: ~
impact: ~
appetiteMinutes: ~
estimateMinutes: 180
actualMinutes: ~
templateId: ~
---

# CodexProvider: parse ~/.codex rollout logs into the canonical usage store

## Goal

Add a `CodexProvider` alongside `ClaudeProvider` in `canonical.py` that parses
Codex rollout logs (`~/.codex/sessions/**/*.jsonl`) and yields the same neutral
`Rec` stream (`Session`/`Turn`/`ToolCall`/`Segment`/`RawEvent`) — proving the
provider abstraction holds: one new class, no schema / `write()` / harness change.
Full spec in [`docs/handoff-codexprovider.md`](../../handoff-codexprovider.md).

## Definition of Done

- `CodexProvider` in `canonical.py`, registered in `default_providers()`.
- Codex fixtures + assertions in `test_canonical.py`; full suite green.
- Real smoke ingest of `~/.codex/sessions` prints sane counts, is idempotent on
  re-run, and lands both providers in one `usage.db` keyed by the `provider` column.
- `docs/architecture.md` change-log + `docs/adding-openai-support.md` note the
  canonical path now has a Codex parser.

## Acceptance Criteria

- [ ] `class CodexProvider` emits the neutral `Rec` stream per the handoff
      mapping table (`session_meta`/`turn_context`/`token_count`/`function_call`/
      `function_call_output`/`message`/`reasoning`/`compacted`/escape-hatch).
- [ ] Fresh-input math is Codex-specific: `input_fresh = input_tokens −
      cached_input_tokens`, `cache_read = cached_input_tokens`, cache writes `0`.
- [ ] `reasoning_output` recorded from `reasoning_output_tokens` and treated as a
      subset of `output` (never added on top).
- [ ] Both `token_count` shapes handled: nested `info.last_token_usage` (delta)
      and old flat/cumulative `total_token_usage` (diff path).
- [ ] `codex_tier()` maps Codex model ids; unknown model → `tier=None` but the
      turn is still recorded (raw model never lost).
- [ ] Failure heuristic on `function_call_output` (`success:false` / non-zero
      `exit_code` / stderr-shaped) → unified `error_class`; `exit_code` captured.
- [ ] `default_providers()` registers `CodexProvider`; `python canonical.py`
      ingests real `~/.codex/sessions` with sane counts and idempotent re-run.
- [ ] `test_canonical.py` gains a `codex_rollout()` fixture + the seven asserts
      from the handoff test plan; `python -m unittest test_canonical` is green.
- [ ] `docs/architecture.md` change-log and `docs/adding-openai-support.md`
      updated to note the canonical Codex parser.

## Test Plan

Mirror the Claude tests in `test_canonical.py` with a `codex_rollout()` fixture
of hand-computable tokens, asserting via SQL: fresh-input math; `reasoning_output`
not folded into `output`; both `token_count` shapes; failing `function_call_output`
→ `ok=0` + right `error_class` + `exit_code`; a `codex exec` file with no
`token_count` events → 0 turns, no crash; mid-session model switch picks up the new
tier; reasoning summary present → `Segment("reasoning")` with text, count-only → not.
Plus the real-corpus smoke ingest asserting idempotent re-run and both providers in one db.

## Notes

First story of E1-F1. ClaudeProvider is the reference to mirror. Blocks
E1-F2-S1 (aggregate flip) and E1-F3-S1 (age-out) — both depend on trusting the
canonical store. See handoff doc §"Roadmap context".

## Activity Log


## 2026-07-10 22:50 UTC -- created
Work-item E1-F1-S1 (story) created with status planned.

## 2026-07-10 23:13 UTC -- status-change
Status planned -> in_progress. delivery started

## 2026-07-10 23:30 UTC -- status-change
Status in_progress -> in_review. PR #2 opened
