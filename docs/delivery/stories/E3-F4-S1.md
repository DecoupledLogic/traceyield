---
id: 01KXBAP4CNS6STRFPS32DHZQ7S
type: story
key: E3-F4-S1
status: planned
assigneeId: ~
queue: ~
externalSystem: ~
externalId: ~
externalUrl: ~
workType: feature
urgency: ~
impact: ~
appetiteMinutes: ~
estimateMinutes: 960
actualMinutes: ~
templateId: ~
---

# Define the installed data-directory policy with env overrides (pipx-safe; never writes into install dir)

## Goal

An explicit, pipx-safe data-directory policy governs where generated state is written, with env overrides.

## Definition of Done

_The conditions that make this shippable._

## Acceptance Criteria

```gherkin
Scenario: Never writes into the install dir
  Given an installed package (pipx)
  When traceyield runs
  Then all generated state is written to the configured data dir, never inside the install location
```

```gherkin
Scenario: Env override
  Given TRACEYIELD_* overrides
  When they are set
  Then the data directory honors them; otherwise it falls back to the documented default
```

## Test Plan

_How delivery is verified._

## Notes

## Activity Log


## 2026-07-12 14:11 UTC -- created
Work-item E3-F4-S1 (story) created with status planned.
